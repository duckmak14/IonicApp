@{
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Bảng kê";
    WebContext db = new WebContext();
    var roles = Roles.GetRolesForUser();
    var userRoleIds = db.WebRoles.Where(x => roles.Contains(x.RoleName)).Select(y => y.RoleId);
    var roleWebModule = db.AccessWebModuleRoles.AsNoTracking().FirstOrDefault(x => userRoleIds.Contains(x.RoleId) && x.WebModule.URL == "/admin/Enumeration");
}
<script type="text/javascript">
    var rowselected = [];
    var isCheckedAll = false;


    //function change_handler(e) {
    //    rowselected = [];
    //    var len = this.select().length;
    //    for (i = 0; i < len; i++) {
    //        rowselected.push(this.dataItem(this.select()[i]).ID);
    //    }
    //}

    function binding_handler(e) {

        //rowselected = [];

        //var grid = $("#grid").data("kendoGrid");
        //var dataSource = grid.dataSource;
        //var recordsOnCurrentView = dataSource.view().length;
        //alert(recordsOnCurrentView);
        //var rowItems = $("#grid").data("kendoGrid").dataSource.data();
        //console.log(rowItems);
        //let now = new Date();
        //for (i = 0; i < recordsOnCurrentView; i++) {
        //    if (rowItems[i].PlanDate.getTime() >= now.getTime()) {
        //        row[i].find(".k-grid-Sửa");
        //        alert("đâs");
        //    }
        //}
    }

    $(document).ready(function () {
        $("#btnadd").click(function () {
            opendlg("/admin/Enumeration/add/");
        });
        $("#btnChangeStatus").click(function () {

            var GetSelected = {};
            GetSelected.ID = [];
            $('input:checkbox[name=checkedNodes]').each(function () {
                if ($(this).is(':checked')) {
                    GetSelected.ID.push($(this).val());
                }
            });
            if (GetSelected.ID.length > 0) {
                console.log(GetSelected);

                $.ajax({
                    url: "@Url.Action("ChangeStatus", "Enumeration")",
                    contentType: "application/json",
                    method: "post",
                    data: JSON.stringify({ model: GetSelected }),
                    success: function (data) {
                        $("#grid").data("kendoGrid").dataSource.read();
                        $('input:checkbox[name=checkedNodes]').each(function () {
                            if ($(this).is(':checked')) {
                                $(this).click();
                            }
                        });
                    },
            error: function (d) {
                console.log(d.responseText);
                          }
                    });
            }
            else {
                alert("Vui lòng chọn để duyệt!");
            }

        });
        $("#btnUnChangeStatus").click(function () {

            var GetSelected = {};
            GetSelected.ID = [];
            $('input:checkbox[name=checkedNodes]').each(function () {
                if ($(this).is(':checked')) {
                    GetSelected.ID.push($(this).val());
                }
            });
            if (GetSelected.ID.length > 0) {
                console.log(GetSelected);

                $.ajax({
                    url: "@Url.Action("UnChangeStatus", "Enumeration")",
                    contentType: "application/json",
                    method: "post",
                    data: JSON.stringify({ model: GetSelected }),
                    success: function (data) {
                        $("#grid").data("kendoGrid").dataSource.read();
                        $('input:checkbox[name=checkedNodes]').each(function () {
                            if ($(this).is(':checked')) {
                                $(this).click();
                            }
                        });
                    },
            error: function (d) {
                console.log(d.responseText);
                          }
                    });
            }
            else {
                alert("Vui lòng chọn để duyệt!");
            }

        });
        $("#export").click(function (e) {

            if (rowselected.length > 0) {
                //alert("Selected values: " + selected.join(","));
                opendlg("/admin/Enumeration/ExportExcel/" + rowselected);
            }
            else {
                alert("Vui lòng chọn để xuất file!");
            }
        });

        $("#upload").click(function (e) {
            opendlg("/admin/Enumeration/UploadExcel");
        });
    });

    function duplicate_handler(e) {
        e.preventDefault();
        var dataItem = this.dataItem($(e.currentTarget).closest("tr"));
        opendlg("/admin/Enumeration/duplicate/" + dataItem.ID);
    }

    function edit_handler(e) {
        e.preventDefault();
        var dataItem = this.dataItem($(e.currentTarget).closest("tr"));
        opendlg("/admin/Enumeration/edit/" + dataItem.ID);
    }

    $(document).ready(function () {

        $("#FieldFilter").keyup(function () {

            var value = $("#FieldFilter").val();


            var int = parseInt(value);
            var float = parseFloat(value);

            $("#grid").data("kendoGrid").dataSource.filter({
                logic: "or",
                filters: [

                    {
                        field: "TrackingCode",
                        operator: "contains",
                        value: value
                    },
                    {
                        field: "Route.RouteCode",
                        operator: "contains",
                        value: value
                    },
                    {
                        field: "Route.StartLocation.LocationName",
                        operator: "contains",
                        value: value
                    },
                    {
                        field: "Route.EndLocation.LocationName",
                        operator: "contains",
                        value: value
                    }, {
                        field: "Vehicle.NumberPlate",
                        operator: "contains",
                        value: value
                    },
                    {
                        field: "Vehicle.VehicleWeight.WeightName",
                        operator: "contains",
                        value: value
                    },
                    {
                        field: "ActualWeight.WeightName",
                        operator: "contains",
                        value: value
                    },
                    {
                        field: "TripCount",
                        operator: "eq",
                        value: int
                    },
                    {
                        field: "SourcePartner.PartnerName",
                        operator: "contains",
                        value: value
                    },
                    {
                        field: "DestinationPartner.PartnerName",
                        operator: "contains",
                        value: value
                    },
                    {
                        field: "UnitPrice",
                        operator: "eq",
                        value: float
                    },
                    {
                        field: "TotalMoney",
                        operator: "eq",
                        value: float
                    },
                    {
                        field: "Note",
                        operator: "contains",
                        value: value
                    }, {
                        field: "StatusString",
                        operator: "contains",
                        value: value
                    }
                ]
            });
        });
    });

    function onDataBound(e) {
        $('#grid .k-grid-content').height(screen.height * 0.5);
    
        $('input[name="checkedNodes"]').each(function () {
            if (rowselected.indexOf(this.value) != -1) {
                $(this).attr('checked', 'checked');
            } else {
                $(this).removeAttr('checked');
            }
        });
        var grid = this;
        let now = new Date();
        grid.tbody.find("tr[role='row']").each(function () {
            var model = grid.dataItem(this);
            var status = $(this).find(".status-style").html();
            if (status == "Chờ duyệt") {
                $(this).find(".status-style").addClass("status-red");
            }
            else {
                $(this).find(".status-style").addClass("status-blue");
            }
        });
    };

</script>
<div class="flex row-quanlyxe block-div">
    <div class="flex">
        <div class="manage-car-search row-plan-drive">
            <form action="">
                <img src="~/Content/themes/admin/img/Vector-kehoach.png" alt="calendar" class="img-box-date-picker img-form-plan-drive img-calander-plan-drive">
                <input type="text" name="daterange" class="box-date-picker" id="time" value="@ViewBag.Date">
            </form>
        </div>
        <div class="manage-car-search row-plan-drive">
            <form action="">
                <img src="~/Content/themes/admin/img/Vector-search1.png" alt="search" class="img-box-search img-form-plan-drive img-search-plan-drive">
                <input id='FieldFilter' type="text" name="" placeholder="Tìm kiếm" class="box-search box-search-quanlyxe box-search-drive-plan input-search-plan-drive">
            </form>
        </div>
    </div>
    <div class="flex block-two-row">
        @if (roleWebModule != null && roleWebModule.Add.HasValue && roleWebModule.Add.Value)
        {
            <div class="btn-download btn-plan-drive" id="export">
                <img src="~/Content/themes/admin/img/Vector-download.png" alt="text-14-700">
                <p class="text-14-500 padding-text-plan-drive">Tải về</p>
            </div>
            <div class="btn-download btn-plan-drive" id="btnChangeStatus">
                <img src="~/Content/themes/admin/img/Vector-accpect2.png" alt="text-14-700">
                <p class="text-14-500 padding-text-plan-drive">Duyệt</p>
            </div>
            <div class="btn-download btn-plan-drive" id="btnUnChangeStatus">
                <img src="~/Content/themes/admin/img/Vector-unapproved1.png" alt="text-14-700">
                <p class="text-14-500 padding-text-plan-drive">Hủy duyệt</p>
            </div>
            <div class="btn-download btn-plan-drive" id="btnadd">
                <img src="~/Content/themes/admin/img/Vector-add-white.png" alt="text-14-700">
                <p class="text-14-500 padding-text-plan-drive">Thêm</p>
            </div>
        }
    </div>
</div>
<div id="daterangepicker"></div>
@if (roleWebModule != null && roleWebModule.View.HasValue && roleWebModule.View.Value)
{
    <div class="row-fluid" id="scroll-table">
        <div class="span12">
            <div class="box">
                <div class="box-content nopadding">
                    @(Html.Kendo().Grid<TransportActualViewModel>()
                .Name("grid")
                .Columns(columns =>
                {
                    columns.Command(command =>
                    {
                        if (roleWebModule != null && roleWebModule.Edit.HasValue && roleWebModule.Edit.Value)
                        {
                            command.Custom(Resources.Common.Edit).Click("edit_handler").HtmlAttributes(new {  title = "Sửa" });
                            command.Custom(Resources.Common.AddItem).Click("duplicate_handler").HtmlAttributes(new { title = "Nhân đôi" }); ;
                        }
                    }).HtmlAttributes(new { style = "text-align:center; display:flex;" }).Width(140).HeaderTemplate("<image src='/Content/themes/admin/img/menu-edit.png'>");
                    columns.Bound("").ClientTemplate("<input name='checkedNodes' type='checkbox' class='k-checkbox' value='#=ID#' onclick='childNodeClick(this)'/>")
                       .HtmlAttributes(new { style = "text-align:center;" })
                       .Filterable(false)
                       .HeaderTemplate("<input type='checkbox' class='checked_all' onclick='checkAll(event)' id='chkAll'/>").Width(70);
                    columns.Bound(p => p.StatusString).Title(WebModels.WebResources.Status).HtmlAttributes(new { style = "text-align:center;", @class = "status-style" }).Width(200);
                    columns.Bound(p => p.ActualDate).Title(WebModels.WebResources.PlanDate).HtmlAttributes(new { style = "text-align:center;", @class = "col-fixed" }).Format("{0:dd-MM-yyyy}").Width(175);
                    columns.Bound(p => p.TrackingCode).Title(WebModels.WebResources.TrackingCode).HtmlAttributes(new { style = "text-align:center;" }).Width(135);
                    columns.Bound(p => p.Route.RouteCode).Title(WebModels.WebResources.RouteCode).HtmlAttributes(new { style = "text-align:center;" }).Width(180);
                    columns.Bound(p => p.Route.StartLocation.LocationName).Title(WebModels.WebResources.StartLocation).Width(200);
                    columns.Bound(p => p.Route.EndLocation.LocationName).Title(WebModels.WebResources.EndLocation).Width(200);
                    columns.Bound(p => p.Vehicle.NumberPlate).Title(WebModels.WebResources.NumberPlate).HtmlAttributes(new { style = "text-align:center;" }).Width(150);
                    columns.Bound(p => p.Vehicle.VehicleWeight.WeightName).Title(WebModels.WebResources.VehicleWeight).HtmlAttributes(new { style = "text-align:center;" }).Width(150);
                    columns.Bound(p => p.ActualWeight.WeightName).Title(WebModels.WebResources.ActualWeight).HtmlAttributes(new { style = "text-align:center;" }).Width(150);
                    columns.Bound(p => p.TripCount).Title(WebModels.WebResources.TripCount).HtmlAttributes(new { style = "text-align:center;" }).Width(120);
                    columns.Bound(p => p.SourcePartner.PartnerName).Title(WebModels.WebResources.SourcePartner).HtmlAttributes(new { style = "text-align:center;" }).Width(170);
                    columns.Bound(p => p.DestinationPartner.PartnerName).Title(WebModels.WebResources.DestinationPartner).HtmlAttributes(new { style = "text-align:center;" }).Width(170);
                    columns.Bound(p => p.UnitPrice).Title(WebModels.WebResources.UnitPrice).Width(100).Format("{0:N0}+ đ").HtmlAttributes(new { style = "text-align:right;" }).Width(180);
                    columns.Bound(p => p.TotalMoney).Title(WebModels.WebResources.TotalMoney).Width(100).Format("{0:N0}+ đ").HtmlAttributes(new { style = "text-align:right;" }).Width(150);
                    columns.Bound(p => p.Note).Title(WebModels.WebResources.Note).Width(200);
                })
                .Scrollable(s => s.Height(550))
                .Pageable()
                .Selectable(select => select.Mode(GridSelectionMode.Single))
                .Sortable()
                .Filterable()
                .Reorderable(reorder => reorder.Columns(true))
                .DataSource(dataSource => dataSource
                    .Ajax()
                    .Model(model => model.Id(p => p.ID))
                    .PageSize(20)
                 .Read(read => read.Action("TransportActualLists_Read", "Enumeration").Data("getTime"))
                     .ServerOperation(false)
                 )
                     .Events(events => events.DataBinding("binding_handler").DataBound("onDataBound")
           )
                     )
                </div>
            </div>

        </div>
    </div>

}
<script type="text/javascript" src="~/Content/themes/admin/js/tableHeadFixer.js"></script>

<script>
    function getDataFilter() {
        var dataSource = $("#grid").data("kendoGrid").dataSource;
        var filters = dataSource.filter();
        var allData = dataSource.data();
        var query = new kendo.data.Query(allData);
        var result = query.filter(filters).data;
        return result;
    }
    function getCheckAll() {
        var dataFilterID = [];
        var dataFilter = getDataFilter();
        var check;
        for (var i = 0; i < dataFilter.length; i++) {
            dataFilterID.push(String(dataFilter[i].ID));
        }
        for (var i = 0; i < dataFilterID.length; i++) {
            var index = rowselected.indexOf(dataFilterID[i]);
            if (index != -1) {
                check = true;
            }
            else {
                check = false;
                break;
            }
        }
        return check;
    }

    function childNodeClick(element) {
        var dataID = getCheckAll();
        var id = element.value;
        if (rowselected.indexOf(id) !== -1) {
            rowselected.splice(rowselected.indexOf(id), 1);
        } else {
            rowselected.push(id);
        }
        isCheckedAll = getCheckAll();
        $('#chkAll').prop('checked', isCheckedAll);


    }

    $(function () {
        $(".k-filter").click(function () {
            setTimeout(function () {
                $(".k-filter-menu .k-button").click(function () {
                    setTimeout(function () {
                        rowselected = [];
                        isCheckedAll = getCheckAll();
                        $('#chkAll').prop('checked', isCheckedAll);
                        $('input[name="checkedNodes"]').each(function () {
                            if (rowselected.indexOf(this.value) != -1) {
                                $(this).attr('checked', 'checked');
                            } else {
                                $(this).removeAttr('checked');
                            }
                        });
                    }, 10)
                })
            }, 100);
        });
    });

    function checkAll(e) {

        var rowItems = getDataFilter();
        isCheckedAll = $(e.target).prop('checked');
        if (isCheckedAll) {
            for (var i = 0; i < rowItems.length; i++) {
                var index = rowselected.indexOf(String(rowItems[i].ID));
                if (index == -1) {
                    rowselected.push(String(rowItems[i].ID));
                }
            }
        }
        else if (!isCheckedAll) {
            var dataFilterID = [];
            var dataFilter = getDataFilter();
            for (var i = 0; i < dataFilter.length; i++) {
                dataFilterID.push(String(dataFilter[i].ID));
            }
            for (var i = 0; i < dataFilterID.length; i++) {
                var index = rowselected.indexOf(dataFilterID[i]);
                if (index != -1) {
                    rowselected.splice(index, 1);
                }
            }
        }

        $('input[name="checkedNodes"]').each(function () {
            $(this).prop('checked', isCheckedAll);
        });
    }


    @*function childNodeClick(element) {
        isCheckedAll = $('input[class="k-checkbox"]:checked').length == $('input[class="k-checkbox"]').length;
        $('#chkAll').prop('checked', isCheckedAll);

        var id = element.value;
        if (rowselected.indexOf(id) !== -1) {
            rowselected.splice(rowselected.indexOf(id), 1);
        } else {
            rowselected.push(id);
        }
        console.log(rowselected);

        //CHECK DOWNLOADEXCEL
         var GetSelected = {};
        GetSelected.ID = [];
        GetSelected.ID = rowselected;
            if (GetSelected.ID.length > 0) {
                $.ajax({
                    url: "@Url.Action("CheckPartner", "Enumeration")",
                    contentType: "application/json",
                    method: "post",
                    data: JSON.stringify({ model: GetSelected }),
                    success: function (data) {

                        if (!data.checkPartner) {
                            $("#export").addClass('disabled');
                            $("#export").prop("onclick", null).off("click");
                        }
                        else {
                            $("#export").removeClass('disabled');
                            $("#export").on("click", function () {
                                    //alert("Selected values: " + selected.join(","));
                                opendlg("/admin/Enumeration/ExportExcel/" + rowselected);
                            })
                        }
                    },
            error: function (d) {
                console.log(d.responseText);
                          }
                    });
            }
            else {
                $("#export").addClass('disabled');
                    $("#export").prop("onclick", null).off("click");
            }

    }*@

    @*function checkAll(e) {
        var rowItems = $("#grid").data("kendoGrid").dataSource.data();

        isCheckedAll = $(e.target).prop('checked');
        if (isCheckedAll) {
            for (var i = 0; i < rowItems.length; i++) {
                rowselected.push(String(rowItems[i].ID));
            }
        }
        else if (!isCheckedAll) {
            rowselected.length = 0;
        }

        $('input[name="checkedNodes"]').each(function () {
            $(this).prop('checked', isCheckedAll);
        });

         //CHECK DOWNLOADEXCEL
         var GetSelected = {};
        GetSelected.ID = [];
        GetSelected.ID = rowselected;
        console.log(rowselected);
            if (GetSelected.ID.length > 0) {
                $.ajax({
                    url: "@Url.Action("CheckPartner", "Enumeration")",
                    contentType: "application/json",
                    method: "post",
                    data: JSON.stringify({ model: GetSelected }),
                    success: function (data) {
                        console.log(data);
                        if (!data.checkPartner) {
                            $("#export").addClass('disabled');
                            $("#export").prop("onclick", null).off("click");
                        }
                        else {
                            $("#export").removeClass('disabled');
                            $("#export").on("click", function () {
                                    //alert("Selected values: " + selected.join(","));
                                opendlg("/admin/Enumeration/ExportExcel/" + rowselected);
                            })
                        }
                    },
            error: function (d) {
                console.log(d.responseText);
                          }
                    });
            }
            else {
                $("#export").addClass('disabled');
                $("#export").prop("onclick", null).off("click");
            }
    }*@


    function getTime(e) {
        return {
            datetime: $("#time").val()
        };
    }

    $('input[name="daterange"]').on('apply.daterangepicker', function (ev, picker) {
        $("#grid").data("kendoGrid").dataSource.read();
    });
</script>

